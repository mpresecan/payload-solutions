/**
 * Footer entry that reopens the consent preferences dialog. Withdrawing consent has to be as easy
 * as giving it, which in practice means a permanent link — so with Payload Consent this renders
 * one, and without it there is nothing to reopen.
 *
 * It renders its own `<li>` (or nothing), so the footer list has no empty row either way.
 */
export function ConsentSettingsLink() {
  return null
}
