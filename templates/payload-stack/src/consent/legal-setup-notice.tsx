/**
 * A development-only note on the homepage explaining how to get the legal pages right.
 *
 * It only has something to say when Payload Consent is installed, because the tooling it points
 * at — the deterministic scan, the compliance profile, the drafts — ships with the plugin.
 */
export function LegalSetupNotice() {
  return null
}
