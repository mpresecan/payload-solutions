/** Shared constants that are safe to import from client components (no server code behind them). */
export const PLUGIN_SLUG = 'plugin-vercel'
export const COMPONENT_PREFIX = '@payload-solutions/plugin-vercel'
export const DEFAULT_VIEW_PATH = '/deployments' as const
export const ENDPOINT_BASE = '/vercel'
